name: Build Plex Companion 1.5 APK

on:
  workflow_dispatch:
  push:
    branches:
      - agent/plex-companion-build

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout build branch
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 16 SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Reconstruct and verify Plex Companion 1.2 base
        run: |
          cat .plex-build/v12.part.00 .plex-build/v12.part.01 .plex-build/v12.part.02 .plex-build/v12.part.03 .plex-build/v12.part.04 .plex-build/v12.part.05 .plex-build/v12.part.060 .plex-build/v12.part.061 .plex-build/v12.part.07 .plex-build/v12.part.08 .plex-build/v12.part.090 .plex-build/v12.part.091 | base64 -d > PlexCompanion-Android-v1.2.zip
          echo "5ca6b31b884bd7e1c4c4e48bf0ab9dc06d51875f44169ed283c0f975bf7c5f79  PlexCompanion-Android-v1.2.zip" | sha256sum -c -
          unzip -q PlexCompanion-Android-v1.2.zip

      - name: Apply SMB compatibility fixes
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('PlexCompanion/app/src/main/java/com/alan/plexcompanion/SmbTransfer.java')
          lines = p.read_text().splitlines()
          lines[91] = '        if (c.username == null || c.username.trim().isEmpty()) return base.withCredentials(new NtlmPasswordAuthenticator("", "GUEST", ""));'
          lines[135] = r'        return value.replaceAll("[<>:\"/\\\\|?*]", "_").replaceAll("\\s+", " ").trim();'
          p.write_text('\n'.join(lines) + '\n')
          PY

      - name: Set permanent app identity
        run: |
          sed -i "s/applicationId 'com.alan.plexcompanion'/applicationId 'com.alan.plexcompanion.release'/" PlexCompanion/app/build.gradle
          sed -i 's/android:name=".MainActivity"/android:name="com.alan.plexcompanion.MainActivity"/' PlexCompanion/app/src/main/AndroidManifest.xml

      - name: Apply Plex folder dropdown v1.3
        run: |
          cat .plex-build/v13.patch.part.00 .plex-build/v13.patch.part.01 .plex-build/v13.patch.part.02 .plex-build/v13.patch.part.03 > /tmp/v13.patch
          patch -p0 < /tmp/v13.patch

      - name: Apply automatic Home and Tailscale routing v1.4
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re, base64, zlib
          text = Path('.github/workflows/build-plex-companion-apk.yml').read_text()
          m = re.search(r"data = '''(.*?)'''", text, re.S)
          if not m:
              raise SystemExit('Could not locate verified v1.4 patch data')
          Path('/tmp/v14.patch').write_bytes(zlib.decompress(base64.b64decode(m.group(1))))
          PY
          patch -p0 < /tmp/v14.patch

      - name: Apply saved iPlayer series and search v1.5
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import base64, zlib, hashlib
          data = (Path('.plex-build/v15.patch.b64.0').read_text() + Path('.plex-build/v15.patch.b64.1').read_text()).strip()
          patch = zlib.decompress(base64.b64decode(data))
          digest = hashlib.sha256(patch).hexdigest()
          expected = 'b2c05e8ee6ffd61a66009d47906ae722fb46de6a8ce6d496f4d4746d380bad2d'
          if digest != expected:
              raise SystemExit(f'v1.5 patch checksum mismatch: {digest}')
          Path('/tmp/v15.patch').write_bytes(patch)
          PY
          patch -p0 < /tmp/v15.patch
          grep -E "versionCode|versionName" PlexCompanion/app/build.gradle

      - name: Build unsigned release APK
        working-directory: PlexCompanion
        run: gradle :app:assembleRelease --stacktrace

      - name: Upload unsigned release APK
        uses: actions/upload-artifact@v4
        with:
          name: PlexCompanion-v1.5-unsigned-release-apk
          path: PlexCompanion/app/build/outputs/apk/release/app-release-unsigned.apk

      - name: Stage Android signing tools
        run: |
          mkdir -p signing-tools/lib
          cp "$ANDROID_HOME/build-tools/36.0.0/apksigner" signing-tools/
          cp "$ANDROID_HOME/build-tools/36.0.0/zipalign" signing-tools/
          cp "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" signing-tools/lib/

      - name: Upload Android signing tools
        uses: actions/upload-artifact@v4
        with:
          name: Android-36-signing-tools-v15
          path: signing-tools
